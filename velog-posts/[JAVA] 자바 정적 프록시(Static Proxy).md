<h1 id="1-프록시를-공부하게-된-배경">1. 프록시를 공부하게 된 배경</h1>
<p>Spring AOP를 이해하기 위해 프록시 개념을 공부하던 중,
먼저 자바의 정적 프록시(Static Proxy) 를 직접 구현해보며 구조를 이해해보기로 했다.</p>
<blockquote>
<p>인터페이스 설계
실제 객체
프록시 객체
클라이언트 코드</p>
</blockquote>
<p>를 직접 작성하며 흐름을 따라가는 방식으로 학습했다.</p>
<hr />
<h2 id="2-공통-인터페이스-정의--singer">2. 공통 인터페이스 정의 – Singer</h2>
<p>프록시 구조의 핵심은
<strong>“프록시와 실제 객체가 같은 타입으로 다뤄질 수 있어야 한다”</strong>는 점이다.</p>
<p>이를 위해 먼저 인터페이스를 정의했다.</p>
<pre><code class="language-java">package ding.co.hellospring.proxy;

public interface Singer {
    void sing();
}</code></pre>
<p>이 인터페이스는</p>
<p>“노래할 수 있다”라는 역할(행위) 만 정의한다.</p>
<p>실제 구현은 알지 못한다.</p>
<h2 id="3-실제-객체real-object--iu">3. 실제 객체(Real Object) – IU</h2>
<p>Singer 인터페이스를 구현하는 실제 객체를 만들었다.</p>
<pre><code class="language-java">package ding.co.hellospring.proxy;

public class IU implements Singer {

    @Override
    public void sing() {
        System.out.println(&quot;노래한다 랄랄랄~~ &quot;);
    }
}</code></pre>
<p>이 클래스는</p>
<p>실제 핵심 로직을 담당한다.</p>
<p>프록시 없이 호출해도 정상 동작한다.</p>
<h2 id="4-프록시-객체--iumanager">4. 프록시 객체 – IUManager</h2>
<p>이제 실제 객체를 직접 호출하지 않고
대신 호출을 위임해주는 프록시 객체를 만들었다.</p>
<pre><code class="language-java">package ding.co.hellospring.proxy;

public class IUManager implements Singer {

    private final Singer realSinger;

    public IUManager(Singer realSinger) {
        this.realSinger = realSinger;
    }

    @Override
    public void sing() {
        System.out.println(&quot;마이크테스트마이크테스트&quot;);
        realSinger.sing();
        System.out.println(&quot;공연 종료 - 감사합니다&quot;);
    }
}</code></pre>
<p>이 구조에서 핵심 포인트</p>
<p>IUManager도 Singer를 구현한다
→ 프록시와 실제 객체를 동일한 타입으로 다룰 수 있음</p>
<p>realSinger 필드를 통해
→ 실제 객체에게 핵심 로직을 위임</p>
<p>프록시는
→ 부가 로직만 담당 (준비, 로그, 정산 등)</p>
<h2 id="5-클라이언트-코드--concertmain">5. 클라이언트 코드 – ConcertMain</h2>
<p>클라이언트는 실제 객체(IU)를 직접 호출하지 않고
프록시를 통해 호출한다.</p>
<pre><code class="language-java">package ding.co.hellospring.proxy;

public class ConcertMain {

    public static void main(String[] args) {

        Singer singer = new IU();                 // 실제 객체
        Singer manager = new IUManager(singer);   // 프록시 객체

        System.out.println(&quot;공연 시작!!&quot;);
        manager.sing();
    }
}</code></pre>
<p>이 코드의 중요한 의미</p>
<blockquote>
<p>Singer manager = new IUManager(singer);</p>
</blockquote>
<p>클라이언트는</p>
<p>IU인지</p>
<p>매니저인지
알 필요가 없다</p>
<p>오직 Singer라는 인터페이스에만 의존한다.</p>
<p>이것이 다형성과 프록시 패턴의 핵심 구조이다.</p>
<h2 id="6-실행-흐름-정리">6. 실행 흐름 정리</h2>
<p>실제 실행 순서는 다음과 같다.</p>
<blockquote>
<p>ConcertMain에서 manager.sing() 호출</p>
</blockquote>
<blockquote>
<p>IUManager.sing() 실행</p>
</blockquote>
<blockquote>
<p>프록시의 부가 로직 실행</p>
</blockquote>
<blockquote>
<p>realSinger.sing() 호출</p>
</blockquote>
<blockquote>
<p>IU.sing()에서 실제 노래 수행</p>
</blockquote>
<blockquote>
<p>프록시의 후처리 로직 실행</p>
</blockquote>
<p>즉,</p>
<p>클라이언트 → 프록시 → 실제 객체</p>
<p>라는 호출 흐름이 만들어진다.</p>
<h2 id="7-정적-프록시의-한계">7. 정적 프록시의 한계</h2>
<p>이 구조를 직접 구현해보며
정적 프록시의 명확한 한계도 드러났다.</p>
<p>1️⃣ 대상마다 프록시 클래스를 만들어야 한다</p>
<p>IU → IUManager</p>
<p>BTS → BTSManager</p>
<p>NewJeans → NewJeansManager</p>
<p>👉 대상이 늘어날수록 프록시 클래스도 함께 증가</p>
<p>2️⃣ 로직은 거의 같은데 클래스만 반복 생성</p>
<p>프록시의 역할은 대부분 다음과 같다.</p>
<pre><code class="language-java">@Override
public void sing() {
    // 공통 로직
    realSinger.sing();
    // 공통 로직
}</code></pre>
<p>대상만 다를 뿐</p>
<p>프록시 로직은 거의 동일</p>
<p>결국
👉 .java 파일을 계속 복사해서 만드는 단순 반복 작업이 된다.</p>
<p>3️⃣ 유지보수 비용 증가</p>
<p>공통 로직 변경 시</p>
<p>모든 프록시 클래스 수정 필요</p>
<p>이는 실무 환경에서
확장성과 유지보수성 측면에서 매우 비효율적이다.</p>
<h2 id="8-의문점">8. 의문점</h2>
<p>정적 프록시를 구현하면서 이런 의문이 들었다.</p>
<p>“로직은 똑같은데
대상만 바뀌는 프록시를
자동으로 만들어줄 수는 없을까?”</p>
<p>이 고민을 해결하기 위해 등장한 개념이 바로
👉 자바의 동적 프록시(Dynamic Proxy) 이다.</p>
<h1 id="9-정리">9. 정리</h1>
<p>정적 프록시를 직접 구현하며 다음을 이해할 수 있었다.</p>
<p>프록시는 실제 객체를 감싸는 구조</p>
<p>프록시와 실제 객체는 같은 인터페이스를 구현</p>
<p>프록시는 부가 로직, 실제 객체는 핵심 로직</p>
<p>정적 프록시는 구조는 명확하지만
확장성과 재사용성에 한계가 있다</p>
<p>이 경험 덕분에
Spring AOP에서 왜 동적 프록시를 사용하는지를
자연스럽게 이해할 수 있는 기반이 되었다.</p>
<p>다음 단계에서는
.java 파일을 만들지 않고도 프록시를 생성하는
자바 동적 프록시(Dynamic Proxy) 와
InvocationHandler 구조를 직접 구현해볼 예정이다.</p>